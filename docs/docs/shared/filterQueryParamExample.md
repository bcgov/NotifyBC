the value of the filter query parameter is URL-encoded stringified JSON object

    ```json
    {
      "where": {
        "created": {
          "$gte": "2023-01-01",
          "$lt": "2024-01-01"
        }
      }
    }
    ```
